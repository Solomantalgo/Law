import { Arrow } from './Shared';
import { practiceAreas } from '../data/siteData';
export default function PracticeAreas() { return <section className="section practice-section" id="practice"><div className="section-heading reveal"><div><p className="eyebrow dark">OUR EXPERTISE</p><h2>Legal expertise for<br /><em>complex decisions.</em></h2></div><p className="heading-note">Focused advice for the matters<br />that move people and business forward.</p></div><div className="practice-list">{practiceAreas.map(([number, title, description]) => <a className="practice-row reveal" href="#contact" key={title}><span className="practice-number">{number}</span><span className="practice-title">{title}</span><span className="practice-description">{description}</span><Arrow /></a>)}</div></section>; }
